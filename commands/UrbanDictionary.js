
const Discord = require("discord.js");
const fetch = require("snekfetch");

module.exports.run = async (bot, message, args) => {
    
    if(!args){
        return msg.channel.reply("Give me something to search");
    }
}

 
module.exports.help = {
  name: "Urban"
}